# github-stars

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

If `GITHUB_TOKEN` is not set, the script will prompt you to use the token from `gh auth token`.
